# ============================================================
# 换行符与二进制文件的统一约定
#
# 为什么需要这个文件：
#   项目最终跑在 Linux 容器里，但代码在 Windows 上编辑。
#   Git 在 Windows 上默认 core.autocrlf=true，会把文本文件在检出时
#   转成 CRLF。一旦仓库里混进 CRLF 的 shell 脚本或配置文件，
#   在 Linux 上会报 "bad interpreter: No such file or directory"
#   这类极难定位的错误（因为报错完全不提换行符）。
#   这里强制所有文本文件在仓库中一律用 LF，从根上避免。
# ============================================================

* text=auto eol=lf

# ---------- 二进制文件：禁止任何转换 ----------
# 漏掉字体的后果很隐蔽：Git 会把 woff2 当文本做换行符替换，
# 文件表面还在、大小也差不多，但字体解析失败，页面图标全变方块。
*.woff2  binary
*.woff   binary
*.ttf    binary
*.eot    binary

*.pdf    binary
*.png    binary
*.jpg    binary
*.jpeg   binary
*.gif    binary
*.ico    binary
*.zip    binary
*.gz     binary

# ---------- 语言统计修正 ----------
# 第三方库不该计入本项目的代码量
frontend/vendor/** linguist-vendored
